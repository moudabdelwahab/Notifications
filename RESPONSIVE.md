# قواعد التوافق مع الشاشات

مرجع مختصر لأي شاشة أو مكوّن جديد في المشروع. الهدف: تجربة واحدة سلسة من الموبايل
للديسكتوب من غير ما نرجع نصلّح كل مرة.

## نقاط التوقف المستخدمة

| النقطة | العرض | الاستخدام |
|---|---|---|
| (افتراضي) | < 640px | الموبايل — **هو الحالة الأساسية**، مش استثناء |
| `sm:` | ≥ 640px | موبايل أفقي / تابلت صغير |
| `lg:` | ≥ 1024px | ديسكتوب — هنا بس تظهر التخطيطات المتعددة الأعمدة |

اكتب تنسيق الموبايل أولاً، وضيف `sm:` و `lg:` فوقه. العكس بيسيب الموبايل مكسور.

## القاعدة الأهم: Master/Detail

أي شاشة فيها **قائمة + تفاصيل** جنب بعض على الديسكتوب لازم تكون **شاشتين منفصلتين**
على الموبايل، مش عمودين فوق بعض.

كان التكديس هو المشكلة الأساسية في صندوق الوارد: فتح محادثة كان بيسيب القائمة فوقها،
فتضطر تنزل طويل جداً عشان توصل للرسائل.

```tsx
// القائمة تختفي لما يتفتح عنصر
<div className={`lg:col-span-2 ${active ? 'hidden lg:block' : 'block'}`}>

// التفاصيل تختفي لما مفيش عنصر مفتوح
<div className={`lg:col-span-3 ${active ? 'block' : 'hidden lg:block'}`}>
```

ولازم يكون فيه **زرار رجوع يظهر على الموبايل فقط** داخل التفاصيل:

```tsx
<Button onClick={() => setActive(null)} className="lg:hidden h-10 w-10 p-0">
  <ArrowRight className="w-5 h-5" />
</Button>
```

> في واجهة RTL، سهم **الرجوع** هو `ArrowRight` مش `ArrowLeft`.

## الارتفاعات: `dvh` مش `vh`

`100vh` على الموبايل بيتجاهل شريط عنوان المتصفح، فآخر جزء من المحتوى بيتقص.
استخدم `dvh` للموبايل وسيب `vh` للديسكتوب:

```tsx
className="max-h-[58dvh] lg:max-h-[55vh] overflow-y-auto"
```

## مساحات اللمس

أقل مساحة مريحة للمس ≈ 40px. أيقونة `w-4 h-4` جوه `p-1` بتطلع 24px — صغيرة جداً.

```tsx
// ❌  <button className="p-1"><Trash2 className="w-4 h-4" /></button>
// ✅
<button className="p-2 -m-1 rounded-lg"><Trash2 className="w-4 h-4" /></button>
```

`-m-1` بيلغي أثر الحشو الزيادة على التخطيط، فالشكل ثابت والمساحة أكبر.

## النصوص الطويلة والروابط

رسائل تليجرام فيها روابط S3 طويلة من غير مسافات. `break-words` لوحده مش كفاية:

```tsx
className="whitespace-pre-wrap break-words [overflow-wrap:anywhere]"
```

وفي القوائم استخدم `truncate` مع `min-w-0` على الأب — من غير `min-w-0` الـ flex
بيرفض يصغّر العنصر و`truncate` مش بيشتغل.

## الرؤوس (Headers)

نصوص الأزرار تختفي على الموبايل وتفضل الأيقونة:

```tsx
<Button>
  <Inbox className="w-4 h-4" />
  <span className="hidden sm:inline">صندوق الوارد</span>
</Button>
```

## المؤشرات المتعددة الخطوات

أكتر من ٤ خطوات مش هتدخل عرض موبايل. استخدم شريط تقدّم + عدّاد على الموبايل،
والدوائر الكاملة من `sm:` وفوق — زي ما في `Onboarding.tsx`.

## الحشو والمسافات

| العنصر | الموبايل | الديسكتوب |
|---|---|---|
| حشو الصفحة | `px-3 py-4` | `sm:px-4 sm:py-8` |
| حشو الكروت | `p-5` | `sm:p-8` |
| المسافات بين الأعمدة | `gap-5` | `sm:gap-8` |

## الترقيم (Pagination)

عرض كل أرقام الصفحات بيكسر السطر على الموبايل — ٥٠٠ إشعار يعني ٢٠ زرار في سطر واحد.
استخدم `client/src/components/Pagination.tsx`: بيعرض الأولى والأخيرة ونافذة حوالين
الصفحة الحالية و`…` بينهم، فالعدد الأقصى ٥ أزرار مهما كان عدد الصفحات.

```tsx
<Pagination page={currentPage} pageCount={pageCount} onChange={setPage} />
```

متكتبش ترقيم من الأول تاني — المكوّن ده مستخدَم في `Inbox.tsx` و`MonitoredChatsPanel.tsx`.

## قبل ما تدفع أي شاشة جديدة

- [ ] جرّبها على عرض **375px** (أصغر موبايل شائع)
- [ ] مفيش تمرير أفقي — الصفحة مش بتتحرك يمين وشمال
- [ ] كل زرار قابل للمس براحة بإصبع
- [ ] القائمة والتفاصيل مش فوق بعض
- [ ] النصوص الطويلة بتلتف، مش بتوسّع الصفحة
- [ ] كل فعل بالماوس ليه مقابل باللمس

الفحص ده اتعمل فعلياً بـ Playwright على ٣٧٥px لصفحات الداشبورد وصندوق الوارد
والإعدادات والإعداد الأولي: `document.documentElement.scrollWidth` لازم يساوي
`window.innerWidth`. لو ضفت شاشة جديدة، قيسها بنفس الطريقة بدل ما تتفرج عليها بس.

> `client/src/index.css` فيه `overflow-x: hidden` على `html` كشبكة أمان — بس دي
> بتخفي المشكلة مش بتحلها. لو لقيت تمرير أفقي، دوّر على العنصر اللي بيتمدد وصلّحه.

## اللمس مقابل الماوس

أي تفاعل بالماوس لازم يكون ليه مقابل يشتغل بالإصبع. `onDoubleClick` و`title`
مش شغالين على اللمس خالص، فلو دول الطريقة الوحيدة تبقى الميزة غير موجودة على
الموبايل — ده اللي حصل مع الرد على رسالة في `ConversationsPanel.tsx`.

| بالماوس | لازم يكون معاه |
|---|---|
| `onDoubleClick` | زرار ظاهر |
| `title="..."` | `aria-label` |
| `hover:` لإظهار عنصر | العنصر ظاهر دايماً على الموبايل |
| Enter للإرسال | زرار إرسال — وعلى الموبايل Enter لازم يفضل سطر جديد (`useIsMobile()`) |

## الخطوط

الواجهة كلها عربي، فالخط لازم يكون فيه حروف عربية. Inter و Poppins مفيهمش،
فكانوا بيسقطوا على خط النظام من غير ما حد ياخد باله. المشروع بيستخدم **Cairo**،
بيتحمّل مرة واحدة من `<link>` في `client/index.html`.

> `@import` في CSS لازم ييجي قبل أي قاعدة تانية، وإلا المتصفح بيتجاهله. متحطّش
> استيراد خطوط في نص `index.css`.
